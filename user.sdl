"A user."
type User {
    id: Int!           # non-nullable integer
    login: String!     # non-nullable string
    firstName: String!
    lastName: String!
}

"The root of it all."
type Query {
    "Returns a list of users."
    users: [User]
}
